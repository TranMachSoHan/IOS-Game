import SwiftUI

struct SettingsView: View {
    @ObservedObject var gameSettings: GameSettings

    var body: some View {
        VStack(alignment: .leading) {
            Text("Settings")
          
        }
    }
}
